spring:
  application:
    name: athleticmeet
  datasource:
    url: jdbc:mysql://localhost:3306/sports_db?useSSL=false&serverTimezone=UTC
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.sports.model

logging:
  level:
    com.sports: DEBUG